'use strict';

/**
 * legal-liquidation service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::legal-liquidation.legal-liquidation');
