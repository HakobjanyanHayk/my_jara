'use strict';

/**
 * service-accounting router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::service-accounting.service-accounting');
