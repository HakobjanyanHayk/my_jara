'use strict';

/**
 * legal-liquidation controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::legal-liquidation.legal-liquidation');
